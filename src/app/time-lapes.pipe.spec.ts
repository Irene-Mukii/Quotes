import { TimeLapesPipe } from './time-lapes.pipe';

describe('TimeLapesPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeLapesPipe();
    expect(pipe).toBeTruthy();
  });
});
