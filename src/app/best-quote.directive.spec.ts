import { bestQuoteDirective } from './best-quote.directive';

describe('HibestQuoteDirective', () => {
  it('should create an instance', () => {
    const directive = new bestQuoteDirective();
    expect(directive).toBeTruthy();
  });
});
